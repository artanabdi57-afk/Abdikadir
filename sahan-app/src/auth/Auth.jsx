import React,{useEffect,useState}from'react'
import{supabase,getAuthRedirectUrl}from'../lib/supabase'
import'../styles/auth.css'
const friendlyError=e=>{const m=e?.message||'Something went wrong. Please try again.';if(/invalid login credentials/i.test(m))return'Email or password is incorrect.';if(/email not confirmed/i.test(m))return'Please verify your email before signing in.';if(/user already registered|already registered/i.test(m))return'An account with this email already exists. Try signing in.';return m}
async function ensureProfile(user){if(!user?.id)return null;const displayName=user.user_metadata?.full_name?.trim()||user.email?.split('@')[0]||'Learner';const{data:existing,error:readError}=await supabase.from('sahan_profiles').select('id,display_name,username,avatar_url,bio,role').eq('id',user.id).maybeSingle();if(readError)throw readError;if(!existing){const{error}=await supabase.from('sahan_profiles').upsert({id:user.id,display_name:displayName,role:'learner'},{onConflict:'id'});if(error)throw error}if(user.user_metadata?.creator_application===true){const{data:application,error:appRead}=await supabase.from('sahan_creator_applications').select('id,status').eq('user_id',user.id).maybeSingle();if(appRead)throw appRead;if(!application){const{error:rpcError}=await supabase.rpc('submit_creator_application',{p_user_id:user.id,p_full_name:displayName,p_email:user.email,p_expertise:'To be completed',p_experience:null});if(rpcError){const{error:appError}=await supabase.from('sahan_creator_applications').insert({user_id:user.id,full_name:displayName,email:user.email,expertise:'To be completed',status:'pending'});if(appError&&!/duplicate key/i.test(appError.message))throw appError}}}const{data,error}=await supabase.from('sahan_profiles').select('id,display_name,username,avatar_url,bio,role').eq('id',user.id).single();if(error)throw error;return data}
export default function Auth({mode='login',onAuthenticated}){const[view,setView]=useState(mode),[name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[confirm,setConfirm]=useState(''),[creator,setCreator]=useState(()=>new URLSearchParams(location.search).get('creator')==='1'),[busy,setBusy]=useState(false),[message,setMessage]=useState(''),[error,setError]=useState('');useEffect(()=>{setView(mode);setError('');setMessage('');if(mode==='signup'&&new URLSearchParams(location.search).get('creator')==='1')setCreator(true)},[mode]);const submit=async e=>{e.preventDefault();setBusy(true);setError('');setMessage('');try{if(view==='signup'){if(!name.trim())throw new Error('Please enter your full name.');if(password.length<6)throw new Error('Your password must be at least 6 characters.');if(password!==confirm)throw new Error('Passwords do not match.');const{data,error:authError}=await supabase.auth.signUp({email:email.trim().toLowerCase(),password,options:{data:{full_name:name.trim(),role:'learner',creator_application:creator},emailRedirectTo:getAuthRedirectUrl('/auth/callback')}});if(authError)throw authError;if(data.user?.email_confirmed_at&&data.session){await ensureProfile(data.user);onAuthenticated?.(data.session)}else{setMessage(creator?'Account created successfully. Verify your email. After verification, your Creator & Instructor application will appear in Admin as Pending.':'Account created. Check your email and click the verification link before signing in.');setView('login')}}else if(view==='login'){const{data,error:authError}=await supabase.auth.signInWithPassword({email:email.trim().toLowerCase(),password});if(authError)throw authError;if(!data.user?.email_confirmed_at){await supabase.auth.signOut();throw new Error('Please verify your email before signing in.')};await ensureProfile(data.user);onAuthenticated?.(data.session)}else if(view==='forgot'){const{error:authError}=await supabase.auth.resetPasswordForEmail(email.trim().toLowerCase(),{redirectTo:getAuthRedirectUrl('/reset-password')});if(authError)throw authError;setMessage('If an account exists for this email, a password reset link has been sent.')}else if(view==='reset'){if(password.length<6)throw new Error('Your password must be at least 6 characters.');if(password!==confirm)throw new Error('Passwords do not match.');const{error:authError}=await supabase.auth.updateUser({password});if(authError)throw authError;setMessage('Password updated successfully. You can now sign in.');await supabase.auth.signOut();setView('login')}}catch(err){setError(friendlyError(err))}finally{setBusy(false)}};const title=view==='signup'?'Create your Sahan account.':view==='forgot'?'Reset your password.':view==='reset'?'Choose a new password.':'Welcome back to Sahan.';return <main className="auth-page"><section className="auth-card"><button className="auth-brand" onClick={()=>location.assign('/')}>S<span>Sahan</span></button><div className="auth-copy"><div className="auth-kicker">SAHAN</div><h1>{title}</h1><p>{view==='signup'?(creator?'Create your account and apply to become a Creator & Instructor.':'Create an account, and optionally request Creator & Instructor access.'):(view==='login'?'Sign in to continue.':'Use your Sahan account securely.')}</p></div>{message&&<div className="auth-success">{message}</div>}{error&&<div className="auth-error">{error}</div>}<form onSubmit={submit} className="auth-form">{view==='signup'&&<><label>Full name<input autoComplete="name" value={name} onChange={e=>setName(e.target.value)} required/></label><label className="creator-choice"><input type="checkbox" checked={creator} onChange={e=>setCreator(e.target.checked)}/><span><b>Apply to become a Creator & Instructor</b><small>{creator?'Your account will be created now. After email verification, your application goes to Sahan Admin for approval.':'Your request goes to Sahan Admin. Admin can approve or reject it.'}</small></span></label></>}{view!=='reset'&&<label>Email<input type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label>}{view!=='forgot'&&<label>Password<input type="password" autoComplete={view==='signup'?'new-password':'current-password'} value={password} onChange={e=>setPassword(e.target.value)} required/></label>}{(view==='signup'||view==='reset')&&<label>Confirm password<input type="password" autoComplete="new-password" value={confirm} onChange={e=>setConfirm(e.target.value)} required/></label>}<button className="auth-submit" disabled={busy}>{busy?'Please wait…':view==='signup'?(creator?'Create account & apply':'Create account'):view==='forgot'?'Send reset link':view==='reset'?'Update password':'Sign in'}</button></form><div className="auth-links">{view==='login'&&<><button onClick={()=>setView('forgot')}>Forgot password?</button><span>New to Sahan?</span><button onClick={()=>setView('signup')}>Create account</button></>}{view==='signup'&&<><span>Already have an account?</span><button onClick={()=>setView('login')}>Sign in</button></>}{view==='forgot'&&<button onClick={()=>setView('login')}>Back to sign in</button>}{view==='reset'&&<button onClick={()=>setView('login')}>Back to sign in</button>}</div></section></main>}
